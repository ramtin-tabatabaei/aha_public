# For the local installation, run: source scripts/activate_local.sh
# Optional manual equivalents, run from the project root.
# This file is documentation and is not auto-loaded.
export AHA_FAILGEN_ROOT="$PWD/external/rlbench-failgen"
export RLBENCH_ROOT="$PWD/external/RLBench"
export COPPELIASIM_ROOT="$PWD/external/CoppeliaSim"
# Optional: defaults to this publication folder's outputs/.
# export AHA_OUTPUT_ROOT=/absolute/path/to/outputs
# Optional: defaults to $AHA_OUTPUT_ROOT/calibration.
# export AHA_CALIBRATION_ROOT=/absolute/path/to/calibration
# Set credentials separately; do not commit them.
# export OPENAI_API_KEY=...
# export ANTHROPIC_API_KEY=...
